# Capability-matrix optional "update" stage fixture (MATRIX_UPDATE=1).
# Mutates only description, which UpdateVpnTunnel PATCHes (no RequiresReplace); re-apply must be clean and re-plan a no-op.
tunnel_description = "Regression VPN tunnel fixture (updated)"
