# Capability-matrix optional "update" stage (MATRIX_UPDATE=1). Mutates only the
# record description (Optional, no RequiresReplace); re-apply succeeds and the
# subsequent re-plan is a clean no-op. name/type/records left unchanged.
record_description = "regr A record (updated)"
