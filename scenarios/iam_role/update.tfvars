# Capability-matrix optional "update" stage fixture (MATRIX_UPDATE=1).
# Mutates only description, which UpdateRole PATCHes (no RequiresReplace); re-apply must be clean and re-plan a no-op.
role_description = "regression test IAM role (updated)"
